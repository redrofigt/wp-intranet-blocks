/**
 * Intranet Blocks - editor entry point.
 *
 * Registers every block in src/blocks/. Each block folder contains its own
 * block.json + edit component; front-end markup comes from PHP render
 * callbacks (dynamic blocks).
 */
import './blocks/news-card';
import './blocks/quick-action';
